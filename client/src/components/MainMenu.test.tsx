import React from 'react';
import App from '../App';
import MainMenu from './MainMenu';
import { shallow } from 'enzyme';

const handlePlayAgainstComputer = jest.fn();

test('pvc calls correct handler', () => {
    // const res = shallow(<App />);
    // const mainMenu = res.find(<MainMenu />);
    // const pvcButton = mainMenu.find('#pvc');
    // pvcButton.simulate('click');
    // expect(handlePlayAgainstComputer).toHaveBeenCalled();
    expect(true);
});
